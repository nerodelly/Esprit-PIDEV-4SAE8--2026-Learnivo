apiVersion: apps/v1
kind: Deployment
metadata:
  name: claims-service
  namespace: learnivo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claims-service
  template:
    metadata:
      labels:
        app: claims-service
    spec:
      containers:
        - name: claims-service
          image: ghcr.io/nerodelly/claims-service:IMAGE_TAG
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: learnivo-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/learnivo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: learnivo-secrets
                  key: SPRING_DATASOURCE_PASSWORD
            - name: SPRING_RABBITMQ_HOST
              value: rabbitmq
          readinessProbe:
            httpGet:
              path: /api/claims/health
              port: 8089
            initialDelaySeconds: 40
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/claims/health
              port: 8089
            initialDelaySeconds: 80
            periodSeconds: 20
