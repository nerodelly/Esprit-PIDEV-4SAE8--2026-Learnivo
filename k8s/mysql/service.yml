apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: learnivo
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None  # headless — stable DNS for JDBC
